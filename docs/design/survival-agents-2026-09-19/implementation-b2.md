# B-2 구현과 검증 — 2026-09-20

공식 Claude 요청 `20260920042729848-d951ed`를 자동 수신한 기존 Codex 대화에서 미확인 여부를 확인하고 구현했다. 기존 구현 worktree와 `feat/survival-local-agents-20260919` 브랜치를 사용했다. Claude는 읽기 전용 리뷰 역할이며, 계약 제안은 `20260920105101564-8b6479`로 전송했다. 발송 자체를 동의나 리뷰 완료로 간주하지 않는다.

## 동작 계약

- 컨셉 24개: 권장 BPM·안무·평가 관점·파트 안내를 포함한다. 새 시즌에서 시드로 10개를 선정하고 순서를 저장한다. 기존 시즌의 컨셉 문자열과 순서는 다시 뽑지 않는다. 권장값은 심사 가중이나 계획 유효성 제한을 바꾸지 않는다.
- 상대 19팀: 강 4 / 중 8 / 약 7, 고강도·승부수·안정 성향. 성향과 컨셉 권장 BPM이 결정적 상대 계획에 반영된다. 공지와 첫 제안 입력에 같은 상대 카드(직전 평균 점수 포함)를 제공한다. 기존 진행 라운드의 상대 규칙은 유지한다.
- 피로 0~100: `practice[5]`는 기존 멤버별 총 PP(합계 12, 최소 1), `recovery[5]`는 그 안에서 회복에 쓸 PP다. 회복 1PP는 피로 12 감소, 남은 PP로 연습한다. 무대 시작 피로의 0.3배가 호흡 부담에 더해진다. 수행 후 리드 4 + 안무 강도 인덱스×4 + 승부수 5 + 수행 강도×2가 피로에 더해진다. 수행 증거와 피로를 함께 저장하므로 심사 재시도가 중복 적용하지 않는다. 모두 게임 창작 수치다.
- 회고: 자연어 `action`을 유지하며 `structuredAction`은 focus, practiceDelta(-2~2), 또는 둘 모두다. 빈 행동은 거절한다. `expectedEffect`는 metric(breath/quality/beatErrorMs/expression)과 direction(up/down)이다. 모든 출력 객체의 필드를 명시한 세 가지 anyOf 형태로 제한한다.
- 가설 관찰: 다음 라운드부터 직전 수행과 비교한다. 지정한 행동이 전부 일치해야 시험됨으로 기록한다. practiceDelta는 멤버별 총 PP의 변화다. 지표가 예상 방향이면 supported, 반대면 contradicted, 같으면 unchanged 관찰만 남긴다. 라운드별 관찰은 중복되지 않는다. 지지 관찰 두 번부터 지지 배지, 한 번은 관찰 1회다. 미시험은 hypothesis이고 생성/마지막 시험 후 3라운드 무관이면 retired다. 계속 시험되는 가설은 관찰을 누적하며 개인 가설 10개 상한을 유지한다. 옛 자연어 가설의 조건을 추측해 검증하지 않는다.
- 성장 화면에 **같은 조건 비교 아님 · 단일 라운드 관찰**을 표시한다. 컨셉·숙련·피로가 달라져 생긴 변화이며 인과효과 확정이 아니다.
- 이전 진행 라운드는 기존 스키마·컨셉·세션 프로필을 유지한다. 새 라운드에서 rulesVersion 2 및 새 멤버 문맥으로 전환한다. 새 호출은 스키마도 저장하여 재시도에 재사용한다. 완료된 호출과 과거 증거는 바꾸지 않는다.
- 시즌 이력은 `/api/history`와 `/api/history/:id` GET 및 화면으로 열람한다. 라운드별 순위·평균·탈락·우리 팀 결과만 제공한다. 세션 ID·프롬프트·자료 초안·명령 기록을 내보내지 않으며 현재 시즌을 교체하거나 모델을 호출하지 않는다.

## 검증

사용 흐름: 컨셉/상대 확인 → 회의 및 합의 → 무대·회고 → 다음 라운드 회복 배분 → 가설 관찰 → 새 시즌 후 이전 시즌 열람.

- `node --test tests/survival/*.test.mjs`: 기존 21개 + B2 9개, 총 30개 통과. 시즌 20→18→…→2→1, 55대진/110수행은 fixture만 사용한다.
- 마지막 스키마의 빈 행동 거부 보완 후 B2 9개 재검증 통과.
- 저장된 컨셉 보존, 티어 구성, 상대 prompt 일치, PP 경계, 피로의 심사 재시도 불변, 관찰 중복 방지·두 번 누적·반박·변화 없음·미시험·retired, 기존 라운드 스키마 유지, 이력 조회의 상태 불변/비공개 정보 제외/외부 Origin 거부를 확인했다.
- ESLint: 작업 worktree에는 node_modules가 없어 기존 프로젝트의 설치본 및 config를 지정했다. 최종 오류 0. `git diff --check` 통과.
- `SURVIVAL_DEPENDENCIES=/Users/admin/workSpace/rescene-game node tools/survival/verify-browser.mjs`: 격리 fixture 서버에서 두 라운드, 회복 폼→API→저장→성장 화면, archive→목록→이전 우리 팀 결과를 확인했다. 현재 시즌 ID 및 모델 호출 수 불변, 브라우저 오류 0, 모바일 가로 넘침 없음. 모바일 캡처도 육안 확인했다.
- 브라우저 증적: `/var/folders/04/8rm5pwr52f12x6zmvdwsdcjr0000gn/T/rescene-browser-J715K6/`의 desktop.png/mobile.png. 임시 시험 자료이며 영구 배포 아티팩트가 아니다.
- B2 실제 Claude/Codex 모델 호출은 0회다. 새 출력 스키마의 실제 모델 응답과 장기 게임 균형은 이번 검증에서 확인하지 않았다.

## 실제 반영 및 회신

- 구현 커밋 `4dd116428b154c61c16e5de3a03a45903677f43a`는 작업 브랜치의 설정된 upstream에 push됐다. PR 머지·배포는 수행하지 않았다.
- 유휴 로컬 서버의 소유 PID와 저장 폴더를 확인한 뒤 같은 구현 worktree로 재시작했다(PID 49473). `http://127.0.0.1:4317/survival.html`, 저장 폴더 `~/Library/Application Support/ResceneSurvival/`. API의 컨셉 24개와 이력 조회를 확인했고, 시즌 미시작 상태를 그대로 보존했다.
- 고정 커밋 Claude 리뷰 요청 `20260920105952665-4fc412`를 발송했다. 이 기록 시점에 실질 리뷰 회신은 확인되지 않았다.
- 최초 요청에 대한 공식 `peer-send reply --ref` 결과는 `20260920110141376-2250ad`다. 구현·시험·push·로컬 반영과 미검증 사항을 함께 회신했다. Claude가 이 회신을 실제 읽었다는 증거는 별도다.
- 기존 수신기 작업에서 실패한 두 호출의 종료는 호스트 반환으로 확인했으나 공식 완료 훅이 해당 실패 형식을 회수하지 못한다. 이 전역 완료 판정은 별도 미완료 보류이며, B2 시험 실패나 미푸시로 취급하지 않는다. 훅 코드·상태를 임의 수정하지 않았다.
